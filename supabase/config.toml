project_id = "luucsmybijcaejhfiwwr"

[functions.invite-user]
verify_jwt = true

[functions.delete-user]
verify_jwt = true

[functions.resend-invite]
verify_jwt = true

[functions.latest-release]
verify_jwt = false

# Chamada pelo front com o JWT do usuário (valida permissão no servidor)
[functions.disparar-agente]
verify_jwt = true

# Chamada pelo workflow do GitHub SEM JWT — a auth é por assinatura HMAC (callback_secret)
[functions.agente-callback]
verify_jwt = false

# Chamada pelo front com o JWT do usuário (valida permissão no servidor)
[functions.gerar-mensagem-cliente]
verify_jwt = true

# Idem: o JWT identifica quem enviou, e é ele que assina o registro da comunicação
[functions.notificar-cliente-demanda]
verify_jwt = true
